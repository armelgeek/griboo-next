/**
 * Utility functions for layer duration calculation in the engine.
 * 
 * Note: Dynamic duration calculations have been removed in favor of explicit durations.
 * All timing calculations are now handled by the TimingManager class.
 */

import { LayerConfig, ShapeLayerConfig } from '../types';

// This file is kept for backward compatibility but functions have been moved to TimingManager

