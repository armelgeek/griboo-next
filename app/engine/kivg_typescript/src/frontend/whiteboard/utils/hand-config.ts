/**
 * Global Hand Configuration System
 * 
 * Re-export shared configuration to ensure consistency between frontend and shared code.
 * This replaces the duplicate implementation that was causing type mismatches.
 */

export * from '../../../shared/config/hand_config';
